import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PolitprivacidadPage } from './politprivacidad.page';

describe('PolitprivacidadPage', () => {
  let component: PolitprivacidadPage;
  let fixture: ComponentFixture<PolitprivacidadPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PolitprivacidadPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
