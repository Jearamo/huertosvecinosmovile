import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AcuerdousuarioPage } from './acuerdousuario.page';

describe('AcuerdousuarioPage', () => {
  let component: AcuerdousuarioPage;
  let fixture: ComponentFixture<AcuerdousuarioPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AcuerdousuarioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
