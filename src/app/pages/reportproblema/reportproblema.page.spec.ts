import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReportproblemaPage } from './reportproblema.page';

describe('ReportproblemaPage', () => {
  let component: ReportproblemaPage;
  let fixture: ComponentFixture<ReportproblemaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReportproblemaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
